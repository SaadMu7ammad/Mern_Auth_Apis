// import { sum } from '../Controllers/userController';
// //.skip .only
// //beforeAll beforeEach
// describe('sum All TestCases', function () {
//   test('return the correct sum', () => {
//     expect(sum(1, 4)).toBe(5);
//   });
//   test('return the correct sum when passing one parameter ', () => {
//     expect(sum(1)).toBe(1);
//   });
//   test('return the correct sum when passing no parameter ', () => {
//     expect(sum()).toBe(0);
//   });
// });

// // expect.extend({
// //   toBeLarger(recieved, target) {
// //     const pass = recieved > target;

// //     if (pass) {
// //       return {
// //         message: () => `Expected ${recieved}to be larger than ${target}`,
// //         pass: true,
// //       };
// //     } else {
// //       return {
// //         message: () => `Error: Expected ${recieved} to be larger than ${target}`,
// //         pass: false,
// //       };
// //     }
// //   },
// // });
// // test('check maded func by saad ', () => {
// //   expect(sum(1,10)).toBeLarger(10);
// // });

// // //expect.anything()>>all passed except the null or undefined
// // //expect.any(Number)>>all Number only
